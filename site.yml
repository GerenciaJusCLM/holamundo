--- 
- name: Deploy Docker image Prueba integración en shell script y PTA (Personal Token Access) como variable .env
  hosts: webserver
  tasks:
    - name: Pull Docker Image
      docker_image:
        name: hello-world
        source: pull
        tag: latest

    - name: Start Docker container
      docker_container:
        name: hello_world_container
        image: hello-world:latest
        state: started



        